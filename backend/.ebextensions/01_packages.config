packages:
  yum:
    git: []
    postgresql-devel: []

container_commands:
  01_make_logs_dir:
    command: "mkdir -p /var/app/current/logs"
    ignoreErrors: true
  02_make_uploads_dir:
    command: "mkdir -p /var/app/current/uploads"
    ignoreErrors: true

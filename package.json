{
  "name": "sengp3-heroku",
  "version": "1.0.0",
  "description": "Heroku deployment for sengp3",
  "scripts": {
    "heroku-postbuild": "cd backend && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}